skill: skills/host/memory-leak/skill.yaml
scenario: 单进程泄漏
expect_path: [classify_shortage, rank_processes, single_leaker]
node_ctx:
  classify_shortage: {slab_unreclaim_mb: 100, mem_total: 16000, available_pct: 10}
  rank_processes:
    mem_total: 16000
    rows:
    - {rss: 10000000, comm: java, pid: 1}
